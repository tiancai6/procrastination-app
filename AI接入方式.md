# 日迹 App · AI 接入方式指南

> 适用版本：Expo SDK54 / RN 0.81（日迹 DailyTrace）
> 适用功能：AI 对话、三餐营养估算、专注分析、**拍照/选图识别体成分报告（OCR）**、联网搜索

---

## 1. App 是怎么接 AI 的（一句话原理）

日迹所有 AI 功能都走 **OpenAI 兼容的 `chat/completions` 接口**（就是发一个 HTTP 请求到各家平台的地址，带上你的 API Key）。

在 App 的「**我的 → 管理 AI 模型**」里，你可以添加**多个**模型（同一品牌也能加好几个，比如 GLM 同时配 `glm-4.7-flash` 和 `glm-5.2`），每个模型有两个角色：

| 角色 | 用途 | 开关 |
|------|------|------|
| **默认文本模型** | 对话、三餐估算、专注分析、联网搜索 | 「设为默认文本模型」 |
| **默认视觉模型** | **发图片时使用**（如拍照/选图识别体成分报告） | 「设为视觉模型」 |

代码会自动按「发没发图片」选对应模型：发图片 → 视觉模型；否则 → 默认文本模型。
所有功能统一从这里读 Key 和地址，**没有 Key 就会提示「请先到『我的 → 管理 AI 模型』添加模型」**。

---

## 2. 在 App 里怎么填（通用步骤）

1. 打开日迹 → 底部「**我的**」→「**管理 AI 模型**」。
2. 点右上角「**+**」新建。
3. 选**品牌**（智谱 GLM / 豆包 / DeepSeek / Gemini）。
4. 粘贴该平台的 **API Key**。
5. 选或手填**模型标识**（如 `glm-4.7-flash`）。
6. 打开需要的开关：
   - ☑ 设为默认文本模型（对话/分析用）
   - ☑ 设为视觉模型（**拍照识别体成分必须用这个**，且模型要支持图片）
   - ☑ 开启联网搜索（按品牌走 web_search / Google grounding）
7. 点「**保存**」。
8. 第一个添加的模型会自动成为默认；之后可随时改默认。

> 老版本在「我的」里有个单独的 API Key 输入框，填了会自动变成一条 GLM 配置，不用管它。

---

## 2.5 免费情况总览（先给结论，2026-08 实测）

你最关心的两个问题——**「哪些免费」「哪些有免费搜索」**，先上一张总表。下面的「免费」指**不用充钱、不绑信用卡**就能用；各平台政策会变，最终以你控制台显示为准。

| 平台 | 模型本身免费吗 | 免费额度（不用充钱） | 有联网搜索吗 | 联网搜索免费吗 | 能做拍照识别(视觉)吗 |
|------|--------------|---------------------|------------|--------------|-------------------|
| **智谱 GLM** | ✅ **永久免费**（flash 系列） | `glm-4.7-flash` / `glm-4-flash` / `glm-4v-flash` 全部长期免费，新户另送千万级赠送 token | ✅ 有 `web_search` | ✅ 随免费模型开启即可（免费） | ✅ `glm-4v-flash` 免费 |
| **豆包 / 火山方舟** | ⚠️ **一次性**免费 | 新户送 **50 万 token**「安心体验」，用完按量（极便宜） | ✅ 有（联网内容插件，ark `web_search`） | ✅ **每月 2 万次免费**（联网内容插件额度，超出按量后付费） | ✅ **全系支持图片**（Seed 2.0 的 mini/lite/pro 都能拍照识别，无需借 GLM） |
| **DeepSeek** | ⚠️ **一次性**免费 | 新户送 **500 万 token / 30 天**，到期按量（很便宜） | ❌ **无** | ❌ 无 | ❌ 无（不能拍照识别） |
| **Gemini** | ✅ **长期免费层**（不绑卡） | Flash：15 RPM / 1500 次天；Pro：5 RPM / 50 次天（每日配额美西午夜重置） | ✅ 有（Google Search Grounding） | ✅ Flash 有**免费 grounding 额度**（约 500~1500 次/天，来源不一） | ✅ 全部支持图片 |

**一句话建议：**
- 想「**模型免费 + 搜索免费 + 拍照免费**」三个都要、又不想折腾网络 → 选 **智谱 GLM**（flash 全免费，最省心）。
- 想免费且国内能直连、搜索也想白嫖 → 选 **豆包**（推理一次性免费 + 搜索每月 2 万次免费 + 视觉可免费）。
- 能搞定代理、想要最强免费多模态 → 选 **Gemini**。
- **DeepSeek** 只适合「纯文本、先试用」，既没有搜索也没有视觉，别拿它做体成分拍照识别。

> ⚠️ 注意「免费额度」和「联网搜索」是**两套独立额度**：比如豆包，那 50 万 token 只抵模型推理、不抵搜索插件费，但搜索自己还有每月 2 万次免费——所以搜索也能免费用，不是「不能开」。（网上另有「豆包搜索开放服务 每月 500 次免费」是另一个独立搜索 API 产品，跟本文用的方舟「联网内容插件」不是一回事，别混。）

---

## 3. 各平台怎么拿 API Key

### 3.1 智谱 GLM（✅ 推荐，国内直连最稳）

- **官网**：<https://open.bigmodel.cn>（或 <https://www.bigmodel.cn>）
- **拿 Key 路径**：登录 → 右上角头像 → **个人中心 / 用户中心** → **API Keys** → **创建 API Key**（复制保存好，只显示一次）。
- **基地址（App 自动填）**：`https://open.bigmodel.cn/api/paas/v4/chat/completions`
- **常用模型标识**：
  - `glm-4.7-flash` —— **永久免费档，最推荐日常用**（200K 上下文）
  - `glm-4-flash` —— 永久免费，轻量（128K）
  - `glm-5.2` —— 更强（可能收费）
  - `glm-4v-flash` —— **视觉模型，支持图片，用于拍照识别体成分（也免费）**
- **免费情况**：`glm-4.7-flash` / `glm-4-flash` / `glm-4v-flash` 这几个 flash 模型**长期免费、不用充钱**；新注册用户还会额外送一笔赠送 token（各渠道说法不一，千万级，以控制台为准）。免费模型有并发/速率限制（如 30 并发、2 req/s），个人用完全够。
- **视觉**：`glm-4v-flash` 支持图片 → 把它设为「视觉模型」即可用体成分 OCR，**且免费**。
- **联网搜索**：内置 `web_search`，开「联网搜索」开关即可；**随免费模型开启不额外收费**（以控制台为准）。

### 3.2 豆包 / 火山方舟（Volcengine Ark）

- **控制台**：<https://console.volcengine.com/ark>
- **有没有免费额度？有，但是「一次性」**：新用户注册 + 实名 + 开通方舟后，送 **一次性 50 万 tokens「安心体验」免费推理额度**（覆盖豆包全系 + 平台上架的 DeepSeek），有「安心模式」可把用量卡在免费额度内、用完即停不扣费。
  - ⚠️ 这 50 万 **只抵普通文本/视觉推理的 token 费用**，**不抵插件调用（联网搜索就是插件）、知识库、Batch**。
  - 价格本身极便宜（Seed 2.0 Mini 输入约 ¥0.000072/百万 tokens），轻度用几乎等于免费。
  - 个别第三方说「每天送 200 万 tokens 永久」，未在官方一手文档确认，以你自己控制台「费用中心 / 用量」显示为准。
- **拿 Key + 建接口（新手最常踩坑：只建了 Key 没建接入点，调用会报 404 `InvalidEndpointOrModel.NotFound`）**：
  1. **实名认证**（硬门槛，没实名连模型都选不了）。
  2. 左侧 **「开通管理」** → 开通你要用的豆包模型（开通不收费）。
  3. 左侧 **「API Key 管理」** → **创建 API Key**（只显示一次，复制存好）。
  4. 左侧 **「在线推理」** → **创建在线推理** → **自定义推理接入点** → 选 Doubao 模型 → 确认。创建完左上角 `ep-` 开头那串就是**接入点 ID（你的「接口」）**，**把它当「模型标识」填进 App**（不是模型名）。
- **基地址（App 自动填）**：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`
- **常用模型标识**：填上一步拿到的 `ep-xxxx` 接入点 ID（推荐，便于在控制台看限流和账单）；或对应 model_id。
- **视觉**：⭐ **Doubao-Seed-2.0 全系（mini / lite / pro）都原生支持图片输入**（含图片理解），所以拍照识别体成分报告**用豆包自己就能做，不必借 GLM**。把任一条豆包模型在 App 里勾选「设为视觉模型」即可；日常最推荐 `doubao-seed-2.0-mini` 或 `doubao-seed-2.0-lite` 当视觉模型（便宜且够用）。
- **联网搜索**：是「**服务组件库**」里的**联网内容插件**（ark `web_search` 工具），开启后模型可联网搜最新信息。
  - ✅ **联网内容插件每月 2 万次免费额度**（超出按量后付费，约几分钱/次，以控制台为准）——和那 50 万「安心体验」token 是两套独立账，token 不抵搜索费，但搜索自己有 2 万次/月免费，**可以放心开**。
  - ⚠️ 想真正用上网搜要做两件事：① 控制台「服务组件库」开通联网内容插件（见下方「联网搜索在哪里开」）；② App 里把该模型「开启联网搜索」开关打开，App 才会发搜索请求。
- **联网搜索在哪里开（两步）**：
  1. **控制台**：在火山方舟控制台顶部搜索框搜「**服务组件库**」→ 进入后找到「**联网内容插件**」→ 点「**开通**」（免费，只是激活能力，不扣钱）。
  2. **App**：日迹「管理 AI 模型」→ 编辑你那条豆包模型 → 打开「**开启联网搜索**」开关 → 保存。

### 3.3 DeepSeek

- **官网**：<https://platform.deepseek.com>
- **拿 Key 路径**：登录 → **API Keys** → **创建 Key**。
- **基地址（App 自动填）**：`https://api.deepseek.com/v1/chat/completions`
- **常用模型标识**：`deepseek-chat`（通用）、`deepseek-reasoner`（推理）。
- **免费情况**：新注册开发者账号送 **一次性 500 万 token（约 $8，30 天内有效）**，**不用绑卡**；用完或过期后转按量付费（deepseek-chat 约 $0.14/$0.28 每百万 token，极便宜，但不再免费）。**没有永久免费模型。**
- **视觉**：❌ **不支持图片** → 不能设为「视觉模型」，体成分拍照识别用它**会报错**。
- **联网搜索**：❌ 无内置搜索。
- **特点**：中文/代码强、成本低；适合纯文本试用，但**没有搜索、没有视觉**。

### 3.4 Google Gemini

- **拿 Key 路径**：<https://aistudio.google.com/apikey> → Google 账号登录 → **Create API Key**。
- **基地址（App 自动填，已用 OpenAI 兼容端点）**：`https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`
- **常用模型标识**：`gemini-2.5-flash`（推荐，免费层 15 RPM / 1500 次天）、`gemini-2.5-pro`、`gemini-2.0-flash`。
- **免费情况**：**长期免费层，不用绑卡**。免费层所有主力模型都能用、输入输出都免费；每日配额在美西时间午夜重置（Flash 约 15 次分 / 1500 次天，Pro 约 5 次分 / 50 次天）。免费层提示词可能被 Google 用于改进产品，介意就开计费。
- **视觉**：✅ 支持图片（体成分 OCR 可用）。
- **联网搜索**：Google Search Grounding（让模型联网搜最新信息并带引用）。✅ **Flash / Flash-Lite 在免费层有免费的 grounding 额度**（各官方来源写 500~1500 次天 不等；付费后超出约 $35/千次）。个别渠道提示免费层可能限制 grounding，以 AI Studio 当前显示为准。
- **⚠️ 注意**：国内网络访问 Google 可能不稳定，需要自备代理/网络才能调通。

---

## 4. 拍照识别体成分（OCR）必须用「视觉模型」

你这次加的功能：在「身体信息」弹窗点 📷拍照识别 / 🖼从相册选择，上传体成分报告截图后自动识别并填入。它**一定要走一个支持图片的模型**。

- ✅ 推荐组合（同一 GLM Key 即可）：
  - 默认文本模型 = `glm-4.7-flash`
  - 视觉默认 = `glm-4v-flash`
- ✅ 也可以：视觉默认用 Gemini（`gemini-2.5-flash`）。
- ❌ **不能用 DeepSeek 做视觉默认**（它没有视觉模型，OCR 会失败）。

设置方法：在「管理 AI 模型」里，把支持图片的模型那条打开「**设为视觉模型**」开关。

---

## 5. 联网搜索（免费情况对照）

> 再次提醒：各平台「模型免费额度」和「搜索免费额度」是**两套独立账**。下表只看「搜索」本身。

| 品牌 | 搜索能力 | 免费吗 | 免费额度 / 超出计费 | 怎么开 |
|------|----------|--------|---------------------|--------|
| 智谱 GLM | 内置 `web_search` | ✅ 随免费模型免费 | 不单独收费（以控制台为准） | 开「联网搜索」开关 |
| 豆包 | 联网内容插件（`web_search`） | ✅ 有免费额度 | **每月 2 万次免费**；超出按量后付费 | 控制台「服务组件库」开通插件 + App 开「联网搜索」开关 |
| Gemini | Google Search Grounding | ✅ 有免费额度 | Flash 免费 grounding 约 500~1500 次天；超出约 $35/千次 | 开「联网搜索」开关 |
| DeepSeek | ❌ 无 | — | 无（需自行接搜索） | — |

---

## 6. 常见问题

- **提示「未配置 API Key / 请先添加模型」**：去「管理 AI 模型」加一个并设为默认。
- **拍照识别报错或没反应**：检查「视觉默认」模型是否支持图片（见第 4 节），DeepSeek 不行。
- **Gemini 调用失败**：多半是国内网络问题，换 GLM 或让手机/网络能访问 Google。
- **Key 泄露**：立刻去对应平台吊销并重建一个新 Key。
- **想换模型**：在「管理 AI 模型」里改默认/视觉默认开关，或新增一条换着用。

---

## 7. 进阶：OpenAI 兼容（自定义品牌）

日迹的底层是 OpenAI 兼容接口，理论上**任何提供 `/chat/completions` 的服务都能接**（如 OpenAI、通义千问、Kimi、Claude 兼容网关等）。
当前 App **内置品牌只有上面 4 个**（智谱 / 豆包 / DeepSeek / Gemini）。若要接别的平台，需要让开发在 `src/utils/modelConfig.ts` 的 `BRAND_PRESETS` 里加一个品牌预设（或以后开放「自定义 baseUrl」输入框）。

> 技术参考：`src/utils/model.ts`（`callModel` 发请求）、`src/utils/modelConfig.ts`（品牌预设与多模型管理）、`src/pages/ModelConfigPage.tsx`（「管理 AI 模型」页面）。
